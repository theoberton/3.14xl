import styles from './styles.module.scss';

function NoEditions() {
	return (
		<div className={styles.noEditionsContainer}>
			<h3>You have no editions yet. Get started by creating one!</h3>
		</div>
	);
}

export default NoEditions;
